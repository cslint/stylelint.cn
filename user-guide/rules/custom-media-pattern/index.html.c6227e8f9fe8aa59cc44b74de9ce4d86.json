{"head":{"description":"custom-media-pattern Specify a pattern for custom media query names. 指定一个自定义媒体查询名称的匹配模式。 Options A string will be translated into a RegExp…","title":"custom-media-pattern"},"body":"<h1 id=\"custom-media-pattern\"><a href=\"#custom-media-pattern\" class=\"phenomic-HeadingAnchor\">#</a>custom-media-pattern</h1>\n<p>Specify a pattern for custom media query names.</p>\n<p>指定一个自定义媒体查询名称的匹配模式。</p>\n<pre><code class=\"hljs   language-css\">@<span class=\"hljs-keyword\">custom</span>-<span class=\"hljs-keyword\">media</span> --foo (max-width: <span class=\"hljs-number\">30em</span>);\n<span class=\"hljs-comment\">/**             ↑\n * The pattern of this */</span></code></pre>\n<h2 id=\"options\"><a href=\"#options\" class=\"phenomic-HeadingAnchor\">#</a>Options</h2>\n<pre><code class=\"hljs language-js\">regex|string</code></pre>\n<p>A string will be translated into a RegExp like so <code>new RegExp(yourString)</code> — so be sure to escape properly.</p>\n<p>字符串将被转换为一个正则表达式 - <code>new RegExp(yourString)</code> - 因此，<em>请确保对属性进行转义</em>。</p>\n<p>Given the string:</p>\n<p>给出：</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-string\">\"foo-.+\"</span></code></pre>\n<p>The following patterns are considered warnings:</p>\n<p>以下模式被认为是个警告：</p>\n<pre><code class=\"hljs  invalid-pattern language-css\">@<span class=\"hljs-keyword\">custom</span>-<span class=\"hljs-keyword\">media</span> --bar (min-width: <span class=\"hljs-number\">30em</span>);</code></pre>\n<p>The following patterns are <em>not</em> considered warnings:</p>\n<p>以下模式正常：</p>\n<pre><code class=\"hljs valid-pattern  language-css\">@<span class=\"hljs-keyword\">custom</span>-<span class=\"hljs-keyword\">media</span> --foo-bar (min-width: <span class=\"hljs-number\">30em</span>);</code></pre>\n","__filename":"user-guide/rules/custom-media-pattern.md","__url":"/user-guide/rules/custom-media-pattern/","__resourceUrl":"/user-guide/rules/custom-media-pattern/index.html","__dataUrl":"/user-guide/rules/custom-media-pattern/index.html.c6227e8f9fe8aa59cc44b74de9ce4d86.json"}