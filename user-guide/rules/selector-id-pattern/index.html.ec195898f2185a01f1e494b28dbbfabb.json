{"head":{"description":"selector-id-pattern Specify a pattern for id selectors. 指定一个 id 选择器的匹配模式。 Options A string will be translated into a RegExp — new…","title":"selector-id-pattern"},"body":"<h1 id=\"selector-id-pattern\"><a href=\"#selector-id-pattern\" class=\"phenomic-HeadingAnchor\">#</a>selector-id-pattern</h1>\n<p>Specify a pattern for id selectors.</p>\n<p>指定一个 id 选择器的匹配模式。</p>\n<pre><code class=\"hljs   language-css\"><span class=\"hljs-selector-class\">.foo</span>, <span class=\"hljs-selector-id\">#bar</span><span class=\"hljs-selector-class\">.baz</span> <span class=\"hljs-selector-tag\">a</span>, <span class=\"hljs-selector-id\">#hoo</span><span class=\"hljs-selector-attr\">[disabled]</span> { <span class=\"hljs-attribute\">color</span>: pink; }\n<span class=\"hljs-comment\">/**   ↑           ↑\n * These id selectors */</span></code></pre>\n<h2 id=\"options\"><a href=\"#options\" class=\"phenomic-HeadingAnchor\">#</a>Options</h2>\n<pre><code class=\"hljs language-js\">regex|string</code></pre>\n<p>A string will be translated into a RegExp — <code>new RegExp(yourString)</code> — so <em>be sure to escape properly</em>.</p>\n<p>字符串将被转换为一个正则表达式 - <code>new RegExp(yourString)</code> - 因此，<em>请确保对属性进行转义</em>。</p>\n<p>The selector value <em>after <code>#</code></em> will be checked. No need to include <code>#</code> in your pattern.</p>\n<p><em><code>#</code> 之后的</em> 选择器的值将被检查。无需把 <code>#</code> 包含在你的匹配模式中。</p>\n<p>Given the string:</p>\n<p>给出：</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-string\">\"foo-[a-z]+\"</span></code></pre>\n<p>The following patterns are considered warnings:</p>\n<p>以下模式被认为是个警告：</p>\n<pre><code class=\"hljs  invalid-pattern language-css\"><span class=\"hljs-selector-id\">#foop</span> {}</code></pre>\n<pre><code class=\"hljs  invalid-pattern language-css\"><span class=\"hljs-selector-id\">#foo-BAR</span> {}</code></pre>\n<pre><code class=\"hljs  invalid-pattern language-css\"><span class=\"hljs-selector-tag\">div</span> > <span class=\"hljs-selector-class\">.zing</span> + <span class=\"hljs-selector-id\">#foo-BAR</span> {}</code></pre>\n<p>The following patterns are <em>not</em> considered warnings:</p>\n<p>以下模式正常：</p>\n<pre><code class=\"hljs valid-pattern  language-css\"><span class=\"hljs-selector-id\">#foo-bar</span> {}</code></pre>\n<pre><code class=\"hljs valid-pattern  language-css\"><span class=\"hljs-selector-tag\">div</span> > <span class=\"hljs-selector-class\">.zing</span> + <span class=\"hljs-selector-id\">#foo-bar</span> {}</code></pre>\n<pre><code class=\"hljs valid-pattern  language-css\"><span class=\"hljs-selector-class\">.foop</span> {}</code></pre>\n<pre><code class=\"hljs valid-pattern  language-css\"><span class=\"hljs-selector-attr\">[foo='bar']</span> {}</code></pre>\n","__filename":"user-guide/rules/selector-id-pattern.md","__url":"/user-guide/rules/selector-id-pattern/","__resourceUrl":"/user-guide/rules/selector-id-pattern/index.html","__dataUrl":"/user-guide/rules/selector-id-pattern/index.html.ec195898f2185a01f1e494b28dbbfabb.json"}